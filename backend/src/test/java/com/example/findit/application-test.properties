# Database - Use an In-Memory dummy or just dummy strings since we mock repositories
spring.datasource.url=jdbc:postgresql://localhost:5432/dummy
spring.datasource.username=dummy
spring.datasource.password=dummy

# JWT - Must be at least 64 characters for HS512
jwt.secret=ThisIsADummySecretKeyThatIsAtLeastSixtyFourCharactersLongForTestingHS512
jwt.expiration=900000

# OAuth2 - Dummy credentials so the Security context can boot
spring.security.oauth2.client.registration.google.client-id=dummy-id
spring.security.oauth2.client.registration.google.client-secret=dummy-secret